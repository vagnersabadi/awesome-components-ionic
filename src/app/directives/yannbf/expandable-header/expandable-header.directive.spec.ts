import { ExpandableHeaderDirective } from './expandable-header.directive';

describe('ExpandableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
